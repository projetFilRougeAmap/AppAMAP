{% extends 'base.html.twig' %}

{% block body %}
    <h1>CategorieProduit list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorieProduit in categorieProduits %}
            <tr>
                <td><a href="{{ path('categorieproduit_show', { 'id': categorieProduit.id }) }}">{{ categorieProduit.id }}</a></td>
                <td>{{ categorieProduit.libelle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('categorieproduit_show', { 'id': categorieProduit.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('categorieproduit_edit', { 'id': categorieProduit.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorieproduit_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
