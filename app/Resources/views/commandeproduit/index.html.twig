{% extends 'base.html.twig' %}

{% block body %}
    <h1>CommandeProduit list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantiteproduit</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commandeProduit in commandeProduits %}
            <tr>
                <td><a href="{{ path('commandeproduit_show', { 'id': commandeProduit.id }) }}">{{ commandeProduit.id }}</a></td>
                <td>{{ commandeProduit.quantiteProduit }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('commandeproduit_show', { 'id': commandeProduit.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('commandeproduit_edit', { 'id': commandeProduit.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commandeproduit_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
