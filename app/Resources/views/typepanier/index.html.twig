{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypePanier list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typePanier in typePaniers %}
            <tr>
                <td><a href="{{ path('typepanier_show', { 'id': typePanier.id }) }}">{{ typePanier.id }}</a></td>
                <td>{{ typePanier.libelle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('typepanier_show', { 'id': typePanier.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('typepanier_edit', { 'id': typePanier.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typepanier_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
