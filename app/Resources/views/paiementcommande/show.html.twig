{% extends 'base.html.twig' %}

{% block body %}
    <h1>PaiementCommande</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiementCommande.id }}</td>
            </tr>
            <tr>
                <th>Datepaiement</th>
                <td>{% if paiementCommande.datePaiement %}{{ paiementCommande.datePaiement|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiementCommande.montant }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('paiementcommande_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('paiementcommande_edit', { 'id': paiementCommande.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
